[package]
name = "bsync-core"
version = "0.1.0"
edition = "2024"
description = "Pure logic core for bsync — P2P clipboard sync (zero I/O deps)"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
blake3 = "1"
base64 = "0.22"
thiserror = "2"
